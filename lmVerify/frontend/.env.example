# Base URL of the local-scraper backend (see /local-scraper). "New scan"
# calls this directly via src/lib/scraperApi.js. Auth and scan history
# (src/lib/api.js) still run on mock data — see the notes in that file.
VITE_API_BASE_URL=http://localhost:5000
